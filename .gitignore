# This repo lives inside the movie library, so ignore everything...
/*

# ...and re-include only the tooling.
!/.gitignore
!/Convert-MkvToMp4.ps1
!/README.md
!/SPEC.md
!/Compare-RecycleBin.ps1
!/AGENTS.md
